{
  "name": "data-transform-orchestrator",
  "version": "0.0.1",
  "description": "Orchestrate multiple data transforms into a system",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive",
    "test:watch": "mocha --recursive --watch",
    "build": "./node_modules/.bin/babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "data",
    "workflow",
    "orchestration"
  ],
  "author": "Luke Purton",
  "license": "ISC",
  "dependencies": {
    "data-transform-descriptor": "0.0.4",
    "lodash": "^4.17.4",
    "toposort": "^1.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
